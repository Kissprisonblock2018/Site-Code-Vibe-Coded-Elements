<meta charset="utf-8"></meta>
<meta name="viewport" content="width=device-width, initial-scale=1"></meta>
<title>Support Center</title>
<meta name="description" content="Support center with tutorials, seller shout outs, and platform updates."></meta>
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin="" />
<link href="https://fonts.googleapis.com/css2?family=Lato:wght@100;300;400;700;900&amp;display=swap" rel="stylesheet" />
<style>
<!--
:root {
  --bg: #ffffff;
  --card: #ffffff;
  --text: #222222;
  --muted: #6b7280;
  --border: #e5e7eb;
  --accent: #58b73a;
  --accent-700: #a91760;
  --chip: #f1f5f9;
  --shadow: 0 10px 30px rgba(2, 6, 23, 0.07);

  --card-pad: 16px;
  --card-gap: 12px;
}

*, *::before, *::after {
  box-sizing: border-box;
}

html, body {
  height: 100%;
}

body {
  margin: 0;
  font-family: 'Lato', sans-serif;
  font-weight: 400;
  color: var(--text);
  background: var(--bg);
  line-height: 1.55;
}

a {
  color: inherit;
  text-decoration: none;
}

img {
  max-width: 100%;
  display: block;
}

.container {
  margin-inline: auto;
  padding: 0 10px;
}

/* Menu Bar */
.menu-bar {
  display: flex;
  gap: 20px;
  justify-content: flex-start;
  align-items: center;
  padding: 20px 0;
  border-bottom: 1px solid var(--border);
  max-width: 100%;
  overflow-x: auto;
  white-space: nowrap;
  scroll-behavior: smooth;
  -webkit-overflow-scrolling: touch;
}

.menu-bar::-webkit-scrollbar {
  height: 8px;
}

.menu-bar::-webkit-scrollbar-thumb {
  background: var(--accent);
  border-radius: 10px;
}

.menu-bar a {
  flex: 0 0 auto;
  padding: 14px 28px;
  font-size: 18px;
  font-weight: 700;
  border-radius: 12px;
  background: var(--card);
  color: var(--accent-700);
  border: 2px solid var(--border);
  cursor: pointer;
  text-decoration: none;
  transition: background 0.2s ease, color 0.2s ease;
}

.menu-bar a:hover {
  background: #f9fafb;
  color: var(--accent-700);
}

html {
  scroll-behavior: smooth;
  scroll-padding-top: 20px;
}

/* Hero Section */
.hero {
  padding: 46px 0 24px;
}

.hero h1 {
  font-size: clamp(28px, 5vw, 44px);
  font-weight: 900;
  line-height: 1.08;
  margin: 0 0 10px;
}

.hero p {
  color: var(--muted);
  font-size: clamp(14px, 2.4vw, 18px);
  font-weight: 300;
  margin: 0;
}

/* Main Layout */
.layout {
  display: grid;
  grid-template-columns: 2fr 350px;
  gap: 30px;
  align-items: start;
}

@media(max-width: 960px) {
  .layout {
    grid-template-columns: 1fr;
  }
}

.main-content {
  display: flex;
  flex-direction: column;
  gap: 50px;
}

/* Section Headers */
.section-head {
  display: flex;
  align-items: end;
  gap: 16px;
  margin: 0 0 14px;
}

.section-head h2 {
  font-size: clamp(20px, 3.4vw, 28px);
  margin: 0;
}

h1, h2, h3 {
  margin: 0 0 16px;
  font-weight: 700;
}

h1 { font-size: clamp(28px, 4.5vw, 40px); font-weight: 900; }
h2 { font-size: clamp(20px, 3.4vw, 28px); }
h3 { font-size: 18px; }

/* Grid & Cards */
.grid {
  display: grid;
  grid-template-columns: repeat(12, 1fr);
  gap: 18px;
  align-items: stretch;
}

.card {
  grid-column: span 4;
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 18px;
  overflow: hidden;
  box-shadow: var(--shadow);
  display: flex;
  flex-direction: column;
  height: 100%;
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.6s ease, transform 0.6s ease;
}

.card.loaded {
  opacity: 1;
  transform: translateY(0);
}

.card .thumb {
  aspect-ratio: 16/9;
  background: linear-gradient(135deg, #e2e8f0, #cbd5e1);
  margin: 0;
  border-bottom: 1px solid var(--border);
}

.card .pad {
  padding: var(--card-pad);
  display: flex;
  flex-direction: column;
  gap: var(--card-gap);
}

.card h3 { margin: 0; font-size: 18px; font-weight: 700; }
.card p  { margin: 0; color: var(--muted); font-size: 14px; }

.card a.cta {
  margin: 0;
  align-self: flex-start;
  font-weight: 700;
  color: var(--accent-700);
}

.card .pad .spacer {
  flex: 1 1 auto;
}

@media (max-width: 980px) {
  .card { grid-column: span 6; }
}

@media (max-width: 640px) {
  .card { grid-column: span 12; }
}

/* Sidebar */
.sidebar {
  position: sticky;
  top: 20px;
  display: flex;
  flex-direction: column;
  gap: 24px;
}

.sidebar-section {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 18px;
  box-shadow: var(--shadow);
  opacity: 0;
  transform: translateX(20px);
  transition: opacity 0.6s ease, transform 0.6s ease;
}

.sidebar-section.loaded {
  opacity: 1;
  transform: translateX(0);
}

.sidebar-section h3 {
  margin: 0 0 12px;
  font-size: 16px;
}

.sidebar-section ul {
  list-style: none;
  padding: 0;
  margin: 0;
}

.sidebar-section li {
  margin: 8px 0;
}

.sidebar-section a {
  color: var(--accent-700);
  font-weight: 600;
}

/* Rankings */
.ranking {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 18px;
  box-shadow: var(--shadow);
  padding: 20px;
  opacity: 0;
  transform: translateX(20px);
  transition: opacity 0.6s ease, transform 0.6s ease;
}

.ranking.loaded {
  opacity: 1;
  transform: translateX(0);
}

.ranking h2 {
  margin: 0 0 16px;
  font-size: 18px;
}

.rank-list {
  margin: 0;
  padding: 0;
  list-style: none;
  max-height: 500px;
  overflow-y: auto;
}

.rank-item {
  display: grid;
  grid-template-columns: 32px 36px 1fr;
  align-items: center;
  gap: 10px;
  padding: 8px;
  border-bottom: 1px dashed var(--border);
}

.rank-item:last-child {
  border-bottom: 0;
}

.badge {
  width: 32px;
  height: 32px;
  border-radius: 8px;
  display: grid;
  place-items: center;
  font-weight: 800;
  font-size: 13px;
  color: #fff;
  background: #58b73a;
}

.logo {
  width: 36px;
  height: 36px;
  border-radius: 8px;
  display: grid;
  place-items: center;
  font-weight: 800;
  font-size: 11px;
  color: var(--text);
  background: #e2e8f0;
  border: 1px solid var(--border);
}

/* Clean headers & buttons */
.section-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
}

.btn-inline {
  padding: 0.5rem 0.9rem;
  border-radius: 999px;
  border: 1px solid var(--muted, #ccc);
  text-decoration: none;
  font-size: 0.95rem;
  line-height: 1;
}

.btn-inline:hover {
  background: #f6f6f6;
}

@media (max-width: 640px) {
  .section-header { flex-wrap: wrap; }
  .btn-inline { margin-left: auto; }
}



/* Continuous Sidebar */
.sidebar {
  position: sticky;
  top: 20px;
  display: flex;
  flex-direction: column;
  gap: 24px;
}

.sidebar-section {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 18px;
  box-shadow: var(--shadow);
  opacity: 0;
  transform: translateX(20px);
  transition: opacity 0.6s ease, transform 0.6s ease;
}

.sidebar-section.loaded {
  opacity: 1;
  transform: translateX(0);
}

.sidebar-section h3 {
  margin: 0 0 12px;
  font-size: 16px;
}

.sidebar-section ul {
  list-style: none;
  padding: 0;
  margin: 0;
}

.sidebar-section li {
  margin: 8px 0;
}

.sidebar-section a {
  color: var(--accent-700);
  font-weight: 600;
}

/* Rankings */
.ranking {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 18px;
  box-shadow: var(--shadow);
  padding: 20px;
  opacity: 0;
  transform: translateX(20px);
  transition: opacity 0.6s ease, transform 0.6s ease;
}

.ranking.loaded {
  opacity: 1;
  transform: translateX(0);
}

.ranking h2 {
  margin: 0 0 16px;
  font-size: 18px;
}

.rank-list {
  margin: 0;
  padding: 0;
  list-style: none;
  max-height: 500px;
  overflow-y: auto;
}

.rank-item {
  display: grid;
  grid-template-columns: 32px 36px 1fr;
  align-items: center;
  gap: 10px;
  padding: 8px;
  border-bottom: 1px dashed var(--border);
}

.rank-item:last-child {
  border-bottom: 0;
}

.badge {
  width: 32px;
  height: 32px;
  border-radius: 8px;
  display: grid;
  place-items: center;
  font-weight: 800;
  font-size: 13px;
  color: #fff;
  background: #58b73a;
}

.logo {
  width: 36px;
  height: 36px;
  border-radius: 8px;
  display: grid;
  place-items: center;
  font-weight: 800;
  font-size: 11px;
  color: var(--text);
  background: #e2e8f0;
  border: 1px solid var(--border);
}

.seller-name {
  font-weight: 700;
  font-size: 13px;
}

.seller-meta {
  font-size: 10px;
  color: var(--muted);
  margin-top: 2px;
}

/* Sales Counter */
.sales-counter {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 18px;
  box-shadow: var(--shadow);
  padding: 20px;
  opacity: 0;
  transform: translateX(20px);
  transition: opacity 0.6s ease, transform 0.6s ease;
}

.sales-counter.loaded {
  opacity: 1;
  transform: translateX(0);
}

.sales-counter h2 {
  margin: 0 0 16px;
  font-size: 18px;
}

.counter {
  font-size: 32px;
  font-weight: 900;
  text-align: center;
  margin-bottom: 8px;
  color: var(--accent);
}

.goal {
  font-size: 13px;
  color: var(--muted);
  text-align: center;
  margin-bottom: 12px;
}

.description {
  font-size: 12px;
  color: var(--text);
  margin-bottom: 16px;
  text-align: center;
  line-height: 1.5;
}

.meter-wrap {
  height: 200px;
  display: flex;
  align-items: flex-end;
  background: linear-gradient(to bottom, #f9fafb, #ffffff);
  border: 3px solid #58b73a;
  border-radius: 16px;
  overflow: hidden;
  position: relative;
  box-shadow: 0 4px 12px rgba(88, 183, 58, 0.15);
  margin-left: 50px;
}

.meter {
  width: 100%;
  height: 0%;
  background: linear-gradient(0deg, #58b73a, #36beba);
  transition: height 1.5s ease-in-out;
  align-self: flex-end;
  position: relative;
  z-index: 1;
  border-radius: 0 0 12px 12px;
}

.hashmarks {
  position: absolute;
  top: 0;
  left: -50px;
  width: 50px;
  height: 100%;
  pointer-events: none;
  z-index: 3;
}

.hashmark {
  position: absolute;
  left: 0;
  width: 45px;
  height: 20px;
  display: flex;
  align-items: center;
  justify-content: flex-end;
  padding-right: 8px;
  font-size: 12px;
  font-weight: 700;
  color: #58b73a;
  transform: translateY(50%);
}

section {
  margin-bottom: 0;
}

.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0,0,0,0);
  white-space: nowrap;
  border: 0;
}

/* header row with button aligned to the right */
.section-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
}

/* simple link-as-button styling; tweak to match your theme */
.btn-inline {
  padding: 0.5rem 0.9rem;
  border-radius: 999px;
  border: 1px solid var(--muted, #ccc);
  text-decoration: none;
  font-size: 0.95rem;
  line-height: 1;
}

.btn-inline:hover {
  background: var(--muted-bg, #f6f6f6);
}

/* keep it neat on small screens */
@media (max-width: 640px) {
  .section-header { flex-wrap: wrap; }
  .btn-inline { margin-left: auto; }
}



-->
</style>
<div class="menu-bar">
  <a href="#categories">Tutorials</a>
  <a href="#shoutouts">Successes</a>
  <a href="https://www.4goodvibesgiftshop.com/about">Our Services</a>
  <a
    href="https://4goodvibes.shop/custom/contact-us"
    target="_blank"
    rel="noopener"
    aria-label="Contact us page"
  >
    Contact Us
  </a>
  <a
    href="https://calendly.com/join-4goodvibes/support"
    class="book-call-btn"
    target="_blank"
    rel="noopener"
    aria-label="Book a call on Calendly"
  >
    Book a Call
  </a>
</div>




<section class="hero container" id="learn" aria-labelledby="hero-title">
  <h1 id="hero-title">What's New. Tutorials. Shout Outs.</h1>
  <p>View guides written for creative entrepreneurs: marketing, operations, fulfillment, policies, taxes, and more.</p>
</section>

<div class="container layout">
  <div class="main-content">
